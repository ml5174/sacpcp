"use strict";
var util_1 = require('./util');
function generator(context, generatorConfig) {
    context = util_1.generateContext(context);
    generatorConfig = util_1.fillConfigDefaults(context, generatorConfig, GENERATOR_TASK_INFO);
    var logger = new util_1.Logger('generator');
    console.log('templatesDir', generatorConfig.templatesDir);
    return Promise.resolve().then(function () {
        return logger.finish();
    }).catch(function (reason) {
        return logger.fail(reason);
    });
}
exports.generator = generator;
var GENERATOR_TASK_INFO = {
    fullArgConfig: '--generator',
    shortArgConfig: '-g',
    envConfig: 'ionic_generator',
    defaultConfigFilename: 'generator.config'
};
