import { BuildContext } from './util';
export declare function generator(context?: BuildContext, generatorConfig?: GeneratorConfig): Promise<boolean>;
export interface GeneratorConfig {
    templatesDir: string;
}
